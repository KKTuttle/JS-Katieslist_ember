<h2>{{category.name}}</h2>

{{#each category.listings as |listing|}}
  <h2>{{listing.title}}</h2>
    <h3>{{listing.description}}</h3>
    <h3>{{listing.note}}</h3>
    <img src="{{listing.image}}" alt="{{listing.title}}"/>
{{/each}}
